apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uptime-mcp.fullname" . }}-test-connection"
  labels:
    {{- include "uptime-mcp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:
        - '--spider'
        - '-S'
        - 'http://{{ include "uptime-mcp.fullname" . }}:{{ .Values.service.port }}/healthz'
