apiVersion: v1
kind: Service
metadata:
  name: uptime-mcp
  labels:
    app: uptime-mcp
spec:
  type: ClusterIP
  selector:
    app: uptime-mcp
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
